enum Role {ADMIN, AUTHOR, READ_WRITE}

let person = {
    role: Role.ADMIN
};

console.log(person.role);
console.log(person.role);
console.log(person.role);
console.log(person.role);
console.log(person.role);